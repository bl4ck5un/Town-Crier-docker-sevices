version: "2"
services:
    tc-core:
        image: "bl4ck5un/tc-core:Debug"
        network_mode: host
        entrypoint: "tc"
        volumes:
          - ./config-rinkeby-sim:/config
          - ./logs:/var/log/tc/
    tc-relay:
        env_file:
          - ./.env
        depends_on:
          - tc-core
        image: "bl4ck5un/tc-relay"
        network_mode: host
        # command: "--dryrun"
        command: "--catchup"
